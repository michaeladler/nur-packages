From 034b4cb9bb64cee626cd7eef965b3b713c18edb1 Mon Sep 17 00:00:00 2001
From: Michael Adler <therisen06@gmail.com>
Date: Wed, 21 Apr 2021 11:05:57 +0200
Subject: [PATCH 1/8] cyclelayout

https://dwm.suckless.org/patches/cyclelayouts/
Signed-off-by: Michael Adler <therisen06@gmail.com>
Signed-off-by: Michael Adler <therisen06@gmail.com>
---
 dwm.c | 18 ++++++++++++++++++
 1 file changed, 18 insertions(+)

diff --git a/dwm.c b/dwm.c
index 93d2423..cb6daf3 100644
--- a/dwm.c
+++ b/dwm.c
@@ -288,6 +288,8 @@ static Client *swallowingclient(Window w);
 static Client *termforwin(const Client *c);
 static pid_t winpid(Window w);
 
+static void cyclelayout(const Arg *arg);
+
 
 /* variables */
 static const char broken[] = "broken";
@@ -2455,6 +2457,22 @@ swallowingclient(Window w)
 	return NULL;
 }
 
+void cyclelayout(const Arg *arg) {
+	Layout *l;
+	for(l = (Layout *)layouts; l != selmon->lt[selmon->sellt]; l++);
+	if(arg->i > 0) {
+		if(l->symbol && (l + 1)->symbol)
+			setlayout(&((Arg) { .v = (l + 1) }));
+		else
+			setlayout(&((Arg) { .v = layouts }));
+	} else {
+		if(l != layouts && (l - 1)->symbol)
+			setlayout(&((Arg) { .v = (l - 1) }));
+		else
+			setlayout(&((Arg) { .v = &layouts[LENGTH(layouts) - 2] }));
+	}
+}
+
 Client *
 wintoclient(Window w)
 {
-- 
2.31.1

